[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"
[project]
name = "healthcare-bronze"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = []

[project.scripts]
healthcare-bronze = "healthcare_bronze.main:main"
healthcare-silver = "healthcare_silver.main:main"
[tool.setuptools.packages.find]
where = ["src"]
