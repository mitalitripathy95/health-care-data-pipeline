the entire project architecture is given in the /Users/mitalitripathy/Documents/kai/mitali/portfolio/3-health care data pipeline/tasks.md file

this project is for elevance health a health care provider

but i can not show or use their data and code so need to build the entire code base with sample data

first you have to act a senior data engineer and plan how can we build the entire project with sample data

but the data we have to create using some python files and then enire code base need to be implemented

we can use the terraform for the infrastructre set up and then need to create all the code go through the project and you will understand the flow and entire plan. so basically i need to create the entire project with synthetic data so that i can use for my portfolio but keep in mind we will use the free azure account credentials

also we want to use databricks, want to use two database one is postgres another is sql server , two different poject have different data those will be pur source data, but as we do not have any data we need to create some python scripts that creates lots of data those we will import it in the postgres and sqlserver but before that you need to go treseacrh about the elevance health based on that you need to create different tables for it and which table will be in whic data base and for each of the data we need to create python script to generate real like data volume should be good enough then you can check the steps in /Users/mitalitripathy/Documents/kai/mitali/portfolio/3-health care data pipeline/tasks.md file azure data factory we can use control table and water marking and then medallion architecture and then end snowflake as warehouse the entire project like spark code also need to use unity catalog lakehouse spark code senstivite data hadling like PHI PII all these also observation pipeline heathmonitoring backfilling startegy all need to implement one by one steps by steps but first plan it and provide all detailed plan with tools involved and all the possbile secnarios
