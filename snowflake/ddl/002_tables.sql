-- Gold tables generated from databricks/src/healthcare_gold/contracts.py.
-- Staging tables are transient and replayable; curated tables are permanent.

-- ---------------------------------------------------------------------------
-- Transient staging tables
-- ---------------------------------------------------------------------------
CREATE TRANSIENT TABLE IF NOT EXISTS RAW_STAGE.STG_DIM_PATIENT (
    SURROGATE_KEY NUMBER(38,0),
    _GOLD_BATCH_ID VARCHAR,
    _IS_INFERRED BOOLEAN,
    EFFECTIVE_FROM TIMESTAMP_NTZ,
    EFFECTIVE_TO TIMESTAMP_NTZ,
    IS_CURRENT BOOLEAN,
    _SOURCE_SYSTEM VARCHAR,
    _SOURCE_SCHEMA VARCHAR,
    _SOURCE_TABLE VARCHAR,
    _SILVER_RECORD_HASH VARCHAR,
    _RECORD_HASH VARCHAR,
    _GOLD_PROCESSED_AT TIMESTAMP_NTZ,
    PATIENT_ID NUMBER(38,0),
    ENTERPRISE_PERSON_ID VARCHAR,
    FIRST_NAME VARCHAR,
    LAST_NAME VARCHAR,
    DATE_OF_BIRTH DATE,
    SEX VARCHAR,
    EMAIL VARCHAR,
    PHONE VARCHAR,
    ADDRESS_LINE1 VARCHAR,
    CITY VARCHAR,
    STATE_CODE VARCHAR,
    POSTAL_CODE VARCHAR,
    SYNTHETIC_NATIONAL_ID VARCHAR,
    _COPY_FILE_NAME VARCHAR,
    _COPY_ROW_NUMBER NUMBER(38,0)
);
CREATE TRANSIENT TABLE IF NOT EXISTS RAW_STAGE.STG_DIM_MEMBER (
    SURROGATE_KEY NUMBER(38,0),
    _GOLD_BATCH_ID VARCHAR,
    _IS_INFERRED BOOLEAN,
    EFFECTIVE_FROM TIMESTAMP_NTZ,
    EFFECTIVE_TO TIMESTAMP_NTZ,
    IS_CURRENT BOOLEAN,
    _SOURCE_SYSTEM VARCHAR,
    _SOURCE_SCHEMA VARCHAR,
    _SOURCE_TABLE VARCHAR,
    _SILVER_RECORD_HASH VARCHAR,
    _RECORD_HASH VARCHAR,
    _GOLD_PROCESSED_AT TIMESTAMP_NTZ,
    MEMBER_ID NUMBER(38,0),
    ENTERPRISE_PERSON_ID VARCHAR,
    FIRST_NAME VARCHAR,
    LAST_NAME VARCHAR,
    DATE_OF_BIRTH DATE,
    SEX VARCHAR,
    MEMBER_NUMBER VARCHAR,
    EMAIL VARCHAR,
    PHONE VARCHAR,
    _COPY_FILE_NAME VARCHAR,
    _COPY_ROW_NUMBER NUMBER(38,0)
);
CREATE TRANSIENT TABLE IF NOT EXISTS RAW_STAGE.STG_DIM_PROVIDER (
    SURROGATE_KEY NUMBER(38,0),
    _GOLD_BATCH_ID VARCHAR,
    _IS_INFERRED BOOLEAN,
    EFFECTIVE_FROM TIMESTAMP_NTZ,
    EFFECTIVE_TO TIMESTAMP_NTZ,
    IS_CURRENT BOOLEAN,
    _SOURCE_SYSTEM VARCHAR,
    _SOURCE_SCHEMA VARCHAR,
    _SOURCE_TABLE VARCHAR,
    _SILVER_RECORD_HASH VARCHAR,
    _RECORD_HASH VARCHAR,
    _GOLD_PROCESSED_AT TIMESTAMP_NTZ,
    PROVIDER_ID NUMBER(38,0),
    PROVIDER_EXTERNAL_ID VARCHAR,
    PROVIDER_NAME VARCHAR,
    SPECIALTY VARCHAR,
    NPI_LIKE VARCHAR,
    _COPY_FILE_NAME VARCHAR,
    _COPY_ROW_NUMBER NUMBER(38,0)
);
CREATE TRANSIENT TABLE IF NOT EXISTS RAW_STAGE.STG_DIM_FACILITY (
    SURROGATE_KEY NUMBER(38,0),
    _GOLD_BATCH_ID VARCHAR,
    _IS_INFERRED BOOLEAN,
    EFFECTIVE_FROM TIMESTAMP_NTZ,
    EFFECTIVE_TO TIMESTAMP_NTZ,
    IS_CURRENT BOOLEAN,
    _SOURCE_SYSTEM VARCHAR,
    _SOURCE_SCHEMA VARCHAR,
    _SOURCE_TABLE VARCHAR,
    _SILVER_RECORD_HASH VARCHAR,
    _RECORD_HASH VARCHAR,
    _GOLD_PROCESSED_AT TIMESTAMP_NTZ,
    FACILITY_ID NUMBER(38,0),
    FACILITY_NAME VARCHAR,
    FACILITY_TYPE VARCHAR,
    CITY VARCHAR,
    STATE_CODE VARCHAR,
    _COPY_FILE_NAME VARCHAR,
    _COPY_ROW_NUMBER NUMBER(38,0)
);
CREATE TRANSIENT TABLE IF NOT EXISTS RAW_STAGE.STG_DIM_PLAN (
    SURROGATE_KEY NUMBER(38,0),
    _GOLD_BATCH_ID VARCHAR,
    _IS_INFERRED BOOLEAN,
    EFFECTIVE_FROM TIMESTAMP_NTZ,
    EFFECTIVE_TO TIMESTAMP_NTZ,
    IS_CURRENT BOOLEAN,
    _SOURCE_SYSTEM VARCHAR,
    _SOURCE_SCHEMA VARCHAR,
    _SOURCE_TABLE VARCHAR,
    _SILVER_RECORD_HASH VARCHAR,
    _RECORD_HASH VARCHAR,
    _GOLD_PROCESSED_AT TIMESTAMP_NTZ,
    PLAN_ID NUMBER(38,0),
    PLAN_CODE VARCHAR,
    PLAN_NAME VARCHAR,
    PLAN_TYPE VARCHAR,
    REGION_CODE VARCHAR,
    _COPY_FILE_NAME VARCHAR,
    _COPY_ROW_NUMBER NUMBER(38,0)
);
CREATE TRANSIENT TABLE IF NOT EXISTS RAW_STAGE.STG_DIM_DATE (
    SURROGATE_KEY NUMBER(38,0),
    _GOLD_BATCH_ID VARCHAR,
    _IS_INFERRED BOOLEAN,
    EFFECTIVE_FROM TIMESTAMP_NTZ,
    EFFECTIVE_TO TIMESTAMP_NTZ,
    IS_CURRENT BOOLEAN,
    _SOURCE_SYSTEM VARCHAR,
    _SOURCE_SCHEMA VARCHAR,
    _SOURCE_TABLE VARCHAR,
    _SILVER_RECORD_HASH VARCHAR,
    _RECORD_HASH VARCHAR,
    _GOLD_PROCESSED_AT TIMESTAMP_NTZ,
    DATE_KEY NUMBER(38,0),
    DATE DATE,
    YEAR NUMBER(38,0),
    QUARTER NUMBER(38,0),
    MONTH NUMBER(38,0),
    MONTH_NAME VARCHAR,
    WEEK_OF_YEAR NUMBER(38,0),
    DAY_OF_MONTH NUMBER(38,0),
    DAY_OF_WEEK NUMBER(38,0),
    DAY_NAME VARCHAR,
    IS_WEEKEND BOOLEAN,
    _COPY_FILE_NAME VARCHAR,
    _COPY_ROW_NUMBER NUMBER(38,0)
);
CREATE TRANSIENT TABLE IF NOT EXISTS RAW_STAGE.STG_DIM_DIAGNOSIS (
    SURROGATE_KEY NUMBER(38,0),
    _GOLD_BATCH_ID VARCHAR,
    _IS_INFERRED BOOLEAN,
    EFFECTIVE_FROM TIMESTAMP_NTZ,
    EFFECTIVE_TO TIMESTAMP_NTZ,
    IS_CURRENT BOOLEAN,
    _SOURCE_SYSTEM VARCHAR,
    _SOURCE_SCHEMA VARCHAR,
    _SOURCE_TABLE VARCHAR,
    _SILVER_RECORD_HASH VARCHAR,
    _RECORD_HASH VARCHAR,
    _GOLD_PROCESSED_AT TIMESTAMP_NTZ,
    DIAGNOSIS_CODE VARCHAR,
    DIAGNOSIS_TYPE VARCHAR,
    _COPY_FILE_NAME VARCHAR,
    _COPY_ROW_NUMBER NUMBER(38,0)
);
CREATE TRANSIENT TABLE IF NOT EXISTS RAW_STAGE.STG_DIM_PROCEDURE (
    SURROGATE_KEY NUMBER(38,0),
    _GOLD_BATCH_ID VARCHAR,
    _IS_INFERRED BOOLEAN,
    EFFECTIVE_FROM TIMESTAMP_NTZ,
    EFFECTIVE_TO TIMESTAMP_NTZ,
    IS_CURRENT BOOLEAN,
    _SOURCE_SYSTEM VARCHAR,
    _SOURCE_SCHEMA VARCHAR,
    _SOURCE_TABLE VARCHAR,
    _SILVER_RECORD_HASH VARCHAR,
    _RECORD_HASH VARCHAR,
    _GOLD_PROCESSED_AT TIMESTAMP_NTZ,
    PROCEDURE_CODE VARCHAR,
    PROCEDURE_DESCRIPTION VARCHAR,
    _COPY_FILE_NAME VARCHAR,
    _COPY_ROW_NUMBER NUMBER(38,0)
);
CREATE TRANSIENT TABLE IF NOT EXISTS RAW_STAGE.STG_DIM_MEDICATION (
    SURROGATE_KEY NUMBER(38,0),
    _GOLD_BATCH_ID VARCHAR,
    _IS_INFERRED BOOLEAN,
    EFFECTIVE_FROM TIMESTAMP_NTZ,
    EFFECTIVE_TO TIMESTAMP_NTZ,
    IS_CURRENT BOOLEAN,
    _SOURCE_SYSTEM VARCHAR,
    _SOURCE_SCHEMA VARCHAR,
    _SOURCE_TABLE VARCHAR,
    _SILVER_RECORD_HASH VARCHAR,
    _RECORD_HASH VARCHAR,
    _GOLD_PROCESSED_AT TIMESTAMP_NTZ,
    MEDICATION_ID NUMBER(38,0),
    MEDICATION_CODE VARCHAR,
    MEDICATION_NAME VARCHAR,
    THERAPEUTIC_CLASS VARCHAR,
    _COPY_FILE_NAME VARCHAR,
    _COPY_ROW_NUMBER NUMBER(38,0)
);
CREATE TRANSIENT TABLE IF NOT EXISTS RAW_STAGE.STG_FACT_ENCOUNTER (
    SURROGATE_KEY NUMBER(38,0),
    _GOLD_BATCH_ID VARCHAR,
    _SOURCE_SYSTEM VARCHAR,
    _SOURCE_SCHEMA VARCHAR,
    _SOURCE_TABLE VARCHAR,
    _SILVER_RECORD_HASH VARCHAR,
    _RECORD_HASH VARCHAR,
    _GOLD_PROCESSED_AT TIMESTAMP_NTZ,
    EFFECTIVE_FROM TIMESTAMP_NTZ,
    EFFECTIVE_TO TIMESTAMP_NTZ,
    IS_CURRENT BOOLEAN,
    _EVENT_FIELD VARCHAR,
    DATE_KEY NUMBER(38,0),
    _COPY_FILE_NAME VARCHAR,
    _COPY_ROW_NUMBER NUMBER(38,0)
);
CREATE TRANSIENT TABLE IF NOT EXISTS RAW_STAGE.STG_FACT_CLAIM (
    SURROGATE_KEY NUMBER(38,0),
    _GOLD_BATCH_ID VARCHAR,
    _SOURCE_SYSTEM VARCHAR,
    _SOURCE_SCHEMA VARCHAR,
    _SOURCE_TABLE VARCHAR,
    _SILVER_RECORD_HASH VARCHAR,
    _RECORD_HASH VARCHAR,
    _GOLD_PROCESSED_AT TIMESTAMP_NTZ,
    EFFECTIVE_FROM TIMESTAMP_NTZ,
    EFFECTIVE_TO TIMESTAMP_NTZ,
    IS_CURRENT BOOLEAN,
    _EVENT_FIELD VARCHAR,
    DATE_KEY NUMBER(38,0),
    _COPY_FILE_NAME VARCHAR,
    _COPY_ROW_NUMBER NUMBER(38,0)
);
CREATE TRANSIENT TABLE IF NOT EXISTS RAW_STAGE.STG_FACT_CLAIM_LINE (
    SURROGATE_KEY NUMBER(38,0),
    _GOLD_BATCH_ID VARCHAR,
    _SOURCE_SYSTEM VARCHAR,
    _SOURCE_SCHEMA VARCHAR,
    _SOURCE_TABLE VARCHAR,
    _SILVER_RECORD_HASH VARCHAR,
    _RECORD_HASH VARCHAR,
    _GOLD_PROCESSED_AT TIMESTAMP_NTZ,
    EFFECTIVE_FROM TIMESTAMP_NTZ,
    EFFECTIVE_TO TIMESTAMP_NTZ,
    IS_CURRENT BOOLEAN,
    _EVENT_FIELD VARCHAR,
    DATE_KEY NUMBER(38,0),
    _COPY_FILE_NAME VARCHAR,
    _COPY_ROW_NUMBER NUMBER(38,0)
);
CREATE TRANSIENT TABLE IF NOT EXISTS RAW_STAGE.STG_FACT_PAYMENT (
    SURROGATE_KEY NUMBER(38,0),
    _GOLD_BATCH_ID VARCHAR,
    _SOURCE_SYSTEM VARCHAR,
    _SOURCE_SCHEMA VARCHAR,
    _SOURCE_TABLE VARCHAR,
    _SILVER_RECORD_HASH VARCHAR,
    _RECORD_HASH VARCHAR,
    _GOLD_PROCESSED_AT TIMESTAMP_NTZ,
    EFFECTIVE_FROM TIMESTAMP_NTZ,
    EFFECTIVE_TO TIMESTAMP_NTZ,
    IS_CURRENT BOOLEAN,
    _EVENT_FIELD VARCHAR,
    DATE_KEY NUMBER(38,0),
    _COPY_FILE_NAME VARCHAR,
    _COPY_ROW_NUMBER NUMBER(38,0)
);
CREATE TRANSIENT TABLE IF NOT EXISTS RAW_STAGE.STG_FACT_AUTHORIZATION (
    SURROGATE_KEY NUMBER(38,0),
    _GOLD_BATCH_ID VARCHAR,
    _SOURCE_SYSTEM VARCHAR,
    _SOURCE_SCHEMA VARCHAR,
    _SOURCE_TABLE VARCHAR,
    _SILVER_RECORD_HASH VARCHAR,
    _RECORD_HASH VARCHAR,
    _GOLD_PROCESSED_AT TIMESTAMP_NTZ,
    EFFECTIVE_FROM TIMESTAMP_NTZ,
    EFFECTIVE_TO TIMESTAMP_NTZ,
    IS_CURRENT BOOLEAN,
    _EVENT_FIELD VARCHAR,
    DATE_KEY NUMBER(38,0),
    _COPY_FILE_NAME VARCHAR,
    _COPY_ROW_NUMBER NUMBER(38,0)
);
CREATE TRANSIENT TABLE IF NOT EXISTS RAW_STAGE.STG_FACT_OBSERVATION (
    SURROGATE_KEY NUMBER(38,0),
    _GOLD_BATCH_ID VARCHAR,
    _SOURCE_SYSTEM VARCHAR,
    _SOURCE_SCHEMA VARCHAR,
    _SOURCE_TABLE VARCHAR,
    _SILVER_RECORD_HASH VARCHAR,
    _RECORD_HASH VARCHAR,
    _GOLD_PROCESSED_AT TIMESTAMP_NTZ,
    EFFECTIVE_FROM TIMESTAMP_NTZ,
    EFFECTIVE_TO TIMESTAMP_NTZ,
    IS_CURRENT BOOLEAN,
    _EVENT_FIELD VARCHAR,
    DATE_KEY NUMBER(38,0),
    _COPY_FILE_NAME VARCHAR,
    _COPY_ROW_NUMBER NUMBER(38,0)
);
CREATE TRANSIENT TABLE IF NOT EXISTS RAW_STAGE.STG_FACT_PRESCRIPTION (
    SURROGATE_KEY NUMBER(38,0),
    _GOLD_BATCH_ID VARCHAR,
    _SOURCE_SYSTEM VARCHAR,
    _SOURCE_SCHEMA VARCHAR,
    _SOURCE_TABLE VARCHAR,
    _SILVER_RECORD_HASH VARCHAR,
    _RECORD_HASH VARCHAR,
    _GOLD_PROCESSED_AT TIMESTAMP_NTZ,
    EFFECTIVE_FROM TIMESTAMP_NTZ,
    EFFECTIVE_TO TIMESTAMP_NTZ,
    IS_CURRENT BOOLEAN,
    _EVENT_FIELD VARCHAR,
    DATE_KEY NUMBER(38,0),
    _COPY_FILE_NAME VARCHAR,
    _COPY_ROW_NUMBER NUMBER(38,0)
);
CREATE TRANSIENT TABLE IF NOT EXISTS RAW_STAGE.STG_BRIDGE_CLAIM_DIAGNOSIS (
    BRIDGE_KEY NUMBER(38,0),
    CLAIM_SK NUMBER(38,0),
    DIAGNOSIS_SK NUMBER(38,0),
    SEQUENCE_NUMBER NUMBER(38,0),
    _GOLD_BATCH_ID VARCHAR,
    EFFECTIVE_FROM TIMESTAMP_NTZ,
    EFFECTIVE_TO TIMESTAMP_NTZ,
    IS_CURRENT BOOLEAN,
    _COPY_FILE_NAME VARCHAR,
    _COPY_ROW_NUMBER NUMBER(38,0)
);
CREATE TRANSIENT TABLE IF NOT EXISTS RAW_STAGE.STG_BRIDGE_PROVIDER_FACILITY (
    BRIDGE_KEY NUMBER(38,0),
    AFFILIATION_ID NUMBER(38,0),
    PROVIDER_SK NUMBER(38,0),
    FACILITY_SK NUMBER(38,0),
    EFFECTIVE_START_DATE DATE,
    EFFECTIVE_END_DATE DATE,
    _GOLD_BATCH_ID VARCHAR,
    EFFECTIVE_FROM TIMESTAMP_NTZ,
    EFFECTIVE_TO TIMESTAMP_NTZ,
    IS_CURRENT BOOLEAN,
    _COPY_FILE_NAME VARCHAR,
    _COPY_ROW_NUMBER NUMBER(38,0)
);
CREATE TRANSIENT TABLE IF NOT EXISTS RAW_STAGE.STG_BRIDGE_MEMBER_ENROLLMENT (
    BRIDGE_KEY NUMBER(38,0),
    ENROLLMENT_ID NUMBER(38,0),
    MEMBER_SK NUMBER(38,0),
    PLAN_SK NUMBER(38,0),
    STATUS VARCHAR,
    COVERAGE_START_DATE DATE,
    COVERAGE_END_DATE DATE,
    _GOLD_BATCH_ID VARCHAR,
    EFFECTIVE_FROM TIMESTAMP_NTZ,
    EFFECTIVE_TO TIMESTAMP_NTZ,
    IS_CURRENT BOOLEAN,
    _COPY_FILE_NAME VARCHAR,
    _COPY_ROW_NUMBER NUMBER(38,0)
);

-- ---------------------------------------------------------------------------
-- Curated tables
-- ---------------------------------------------------------------------------
CREATE TABLE IF NOT EXISTS CURATED.DIM_PATIENT (
    SURROGATE_KEY NUMBER(38,0),
    _GOLD_BATCH_ID VARCHAR,
    _IS_INFERRED BOOLEAN,
    EFFECTIVE_FROM TIMESTAMP_NTZ,
    EFFECTIVE_TO TIMESTAMP_NTZ,
    IS_CURRENT BOOLEAN,
    _SOURCE_SYSTEM VARCHAR,
    _SOURCE_SCHEMA VARCHAR,
    _SOURCE_TABLE VARCHAR,
    _SILVER_RECORD_HASH VARCHAR,
    _RECORD_HASH VARCHAR,
    _GOLD_PROCESSED_AT TIMESTAMP_NTZ,
    PATIENT_ID NUMBER(38,0),
    ENTERPRISE_PERSON_ID VARCHAR,
    FIRST_NAME VARCHAR,
    LAST_NAME VARCHAR,
    DATE_OF_BIRTH DATE,
    SEX VARCHAR,
    EMAIL VARCHAR,
    PHONE VARCHAR,
    ADDRESS_LINE1 VARCHAR,
    CITY VARCHAR,
    STATE_CODE VARCHAR,
    POSTAL_CODE VARCHAR,
    SYNTHETIC_NATIONAL_ID VARCHAR
);
CREATE TABLE IF NOT EXISTS CURATED.DIM_MEMBER (
    SURROGATE_KEY NUMBER(38,0),
    _GOLD_BATCH_ID VARCHAR,
    _IS_INFERRED BOOLEAN,
    EFFECTIVE_FROM TIMESTAMP_NTZ,
    EFFECTIVE_TO TIMESTAMP_NTZ,
    IS_CURRENT BOOLEAN,
    _SOURCE_SYSTEM VARCHAR,
    _SOURCE_SCHEMA VARCHAR,
    _SOURCE_TABLE VARCHAR,
    _SILVER_RECORD_HASH VARCHAR,
    _RECORD_HASH VARCHAR,
    _GOLD_PROCESSED_AT TIMESTAMP_NTZ,
    MEMBER_ID NUMBER(38,0),
    ENTERPRISE_PERSON_ID VARCHAR,
    FIRST_NAME VARCHAR,
    LAST_NAME VARCHAR,
    DATE_OF_BIRTH DATE,
    SEX VARCHAR,
    MEMBER_NUMBER VARCHAR,
    EMAIL VARCHAR,
    PHONE VARCHAR
);
CREATE TABLE IF NOT EXISTS CURATED.DIM_PROVIDER (
    SURROGATE_KEY NUMBER(38,0),
    _GOLD_BATCH_ID VARCHAR,
    _IS_INFERRED BOOLEAN,
    EFFECTIVE_FROM TIMESTAMP_NTZ,
    EFFECTIVE_TO TIMESTAMP_NTZ,
    IS_CURRENT BOOLEAN,
    _SOURCE_SYSTEM VARCHAR,
    _SOURCE_SCHEMA VARCHAR,
    _SOURCE_TABLE VARCHAR,
    _SILVER_RECORD_HASH VARCHAR,
    _RECORD_HASH VARCHAR,
    _GOLD_PROCESSED_AT TIMESTAMP_NTZ,
    PROVIDER_ID NUMBER(38,0),
    PROVIDER_EXTERNAL_ID VARCHAR,
    PROVIDER_NAME VARCHAR,
    SPECIALTY VARCHAR,
    NPI_LIKE VARCHAR
);
CREATE TABLE IF NOT EXISTS CURATED.DIM_FACILITY (
    SURROGATE_KEY NUMBER(38,0),
    _GOLD_BATCH_ID VARCHAR,
    _IS_INFERRED BOOLEAN,
    EFFECTIVE_FROM TIMESTAMP_NTZ,
    EFFECTIVE_TO TIMESTAMP_NTZ,
    IS_CURRENT BOOLEAN,
    _SOURCE_SYSTEM VARCHAR,
    _SOURCE_SCHEMA VARCHAR,
    _SOURCE_TABLE VARCHAR,
    _SILVER_RECORD_HASH VARCHAR,
    _RECORD_HASH VARCHAR,
    _GOLD_PROCESSED_AT TIMESTAMP_NTZ,
    FACILITY_ID NUMBER(38,0),
    FACILITY_NAME VARCHAR,
    FACILITY_TYPE VARCHAR,
    CITY VARCHAR,
    STATE_CODE VARCHAR
);
CREATE TABLE IF NOT EXISTS CURATED.DIM_PLAN (
    SURROGATE_KEY NUMBER(38,0),
    _GOLD_BATCH_ID VARCHAR,
    _IS_INFERRED BOOLEAN,
    EFFECTIVE_FROM TIMESTAMP_NTZ,
    EFFECTIVE_TO TIMESTAMP_NTZ,
    IS_CURRENT BOOLEAN,
    _SOURCE_SYSTEM VARCHAR,
    _SOURCE_SCHEMA VARCHAR,
    _SOURCE_TABLE VARCHAR,
    _SILVER_RECORD_HASH VARCHAR,
    _RECORD_HASH VARCHAR,
    _GOLD_PROCESSED_AT TIMESTAMP_NTZ,
    PLAN_ID NUMBER(38,0),
    PLAN_CODE VARCHAR,
    PLAN_NAME VARCHAR,
    PLAN_TYPE VARCHAR,
    REGION_CODE VARCHAR
);
CREATE TABLE IF NOT EXISTS CURATED.DIM_DATE (
    SURROGATE_KEY NUMBER(38,0),
    _GOLD_BATCH_ID VARCHAR,
    _IS_INFERRED BOOLEAN,
    EFFECTIVE_FROM TIMESTAMP_NTZ,
    EFFECTIVE_TO TIMESTAMP_NTZ,
    IS_CURRENT BOOLEAN,
    _SOURCE_SYSTEM VARCHAR,
    _SOURCE_SCHEMA VARCHAR,
    _SOURCE_TABLE VARCHAR,
    _SILVER_RECORD_HASH VARCHAR,
    _RECORD_HASH VARCHAR,
    _GOLD_PROCESSED_AT TIMESTAMP_NTZ,
    DATE_KEY NUMBER(38,0),
    DATE DATE,
    YEAR NUMBER(38,0),
    QUARTER NUMBER(38,0),
    MONTH NUMBER(38,0),
    MONTH_NAME VARCHAR,
    WEEK_OF_YEAR NUMBER(38,0),
    DAY_OF_MONTH NUMBER(38,0),
    DAY_OF_WEEK NUMBER(38,0),
    DAY_NAME VARCHAR,
    IS_WEEKEND BOOLEAN
);
CREATE TABLE IF NOT EXISTS CURATED.DIM_DIAGNOSIS (
    SURROGATE_KEY NUMBER(38,0),
    _GOLD_BATCH_ID VARCHAR,
    _IS_INFERRED BOOLEAN,
    EFFECTIVE_FROM TIMESTAMP_NTZ,
    EFFECTIVE_TO TIMESTAMP_NTZ,
    IS_CURRENT BOOLEAN,
    _SOURCE_SYSTEM VARCHAR,
    _SOURCE_SCHEMA VARCHAR,
    _SOURCE_TABLE VARCHAR,
    _SILVER_RECORD_HASH VARCHAR,
    _RECORD_HASH VARCHAR,
    _GOLD_PROCESSED_AT TIMESTAMP_NTZ,
    DIAGNOSIS_CODE VARCHAR,
    DIAGNOSIS_TYPE VARCHAR
);
CREATE TABLE IF NOT EXISTS CURATED.DIM_PROCEDURE (
    SURROGATE_KEY NUMBER(38,0),
    _GOLD_BATCH_ID VARCHAR,
    _IS_INFERRED BOOLEAN,
    EFFECTIVE_FROM TIMESTAMP_NTZ,
    EFFECTIVE_TO TIMESTAMP_NTZ,
    IS_CURRENT BOOLEAN,
    _SOURCE_SYSTEM VARCHAR,
    _SOURCE_SCHEMA VARCHAR,
    _SOURCE_TABLE VARCHAR,
    _SILVER_RECORD_HASH VARCHAR,
    _RECORD_HASH VARCHAR,
    _GOLD_PROCESSED_AT TIMESTAMP_NTZ,
    PROCEDURE_CODE VARCHAR,
    PROCEDURE_DESCRIPTION VARCHAR
);
CREATE TABLE IF NOT EXISTS CURATED.DIM_MEDICATION (
    SURROGATE_KEY NUMBER(38,0),
    _GOLD_BATCH_ID VARCHAR,
    _IS_INFERRED BOOLEAN,
    EFFECTIVE_FROM TIMESTAMP_NTZ,
    EFFECTIVE_TO TIMESTAMP_NTZ,
    IS_CURRENT BOOLEAN,
    _SOURCE_SYSTEM VARCHAR,
    _SOURCE_SCHEMA VARCHAR,
    _SOURCE_TABLE VARCHAR,
    _SILVER_RECORD_HASH VARCHAR,
    _RECORD_HASH VARCHAR,
    _GOLD_PROCESSED_AT TIMESTAMP_NTZ,
    MEDICATION_ID NUMBER(38,0),
    MEDICATION_CODE VARCHAR,
    MEDICATION_NAME VARCHAR,
    THERAPEUTIC_CLASS VARCHAR
);
CREATE TABLE IF NOT EXISTS CURATED.FACT_ENCOUNTER (
    SURROGATE_KEY NUMBER(38,0),
    _GOLD_BATCH_ID VARCHAR,
    _SOURCE_SYSTEM VARCHAR,
    _SOURCE_SCHEMA VARCHAR,
    _SOURCE_TABLE VARCHAR,
    _SILVER_RECORD_HASH VARCHAR,
    _RECORD_HASH VARCHAR,
    _GOLD_PROCESSED_AT TIMESTAMP_NTZ,
    EFFECTIVE_FROM TIMESTAMP_NTZ,
    EFFECTIVE_TO TIMESTAMP_NTZ,
    IS_CURRENT BOOLEAN,
    _EVENT_FIELD VARCHAR,
    DATE_KEY NUMBER(38,0)
);
CREATE TABLE IF NOT EXISTS CURATED.FACT_CLAIM (
    SURROGATE_KEY NUMBER(38,0),
    _GOLD_BATCH_ID VARCHAR,
    _SOURCE_SYSTEM VARCHAR,
    _SOURCE_SCHEMA VARCHAR,
    _SOURCE_TABLE VARCHAR,
    _SILVER_RECORD_HASH VARCHAR,
    _RECORD_HASH VARCHAR,
    _GOLD_PROCESSED_AT TIMESTAMP_NTZ,
    EFFECTIVE_FROM TIMESTAMP_NTZ,
    EFFECTIVE_TO TIMESTAMP_NTZ,
    IS_CURRENT BOOLEAN,
    _EVENT_FIELD VARCHAR,
    DATE_KEY NUMBER(38,0)
);
CREATE TABLE IF NOT EXISTS CURATED.FACT_CLAIM_LINE (
    SURROGATE_KEY NUMBER(38,0),
    _GOLD_BATCH_ID VARCHAR,
    _SOURCE_SYSTEM VARCHAR,
    _SOURCE_SCHEMA VARCHAR,
    _SOURCE_TABLE VARCHAR,
    _SILVER_RECORD_HASH VARCHAR,
    _RECORD_HASH VARCHAR,
    _GOLD_PROCESSED_AT TIMESTAMP_NTZ,
    EFFECTIVE_FROM TIMESTAMP_NTZ,
    EFFECTIVE_TO TIMESTAMP_NTZ,
    IS_CURRENT BOOLEAN,
    _EVENT_FIELD VARCHAR,
    DATE_KEY NUMBER(38,0)
);
CREATE TABLE IF NOT EXISTS CURATED.FACT_PAYMENT (
    SURROGATE_KEY NUMBER(38,0),
    _GOLD_BATCH_ID VARCHAR,
    _SOURCE_SYSTEM VARCHAR,
    _SOURCE_SCHEMA VARCHAR,
    _SOURCE_TABLE VARCHAR,
    _SILVER_RECORD_HASH VARCHAR,
    _RECORD_HASH VARCHAR,
    _GOLD_PROCESSED_AT TIMESTAMP_NTZ,
    EFFECTIVE_FROM TIMESTAMP_NTZ,
    EFFECTIVE_TO TIMESTAMP_NTZ,
    IS_CURRENT BOOLEAN,
    _EVENT_FIELD VARCHAR,
    DATE_KEY NUMBER(38,0)
);
CREATE TABLE IF NOT EXISTS CURATED.FACT_AUTHORIZATION (
    SURROGATE_KEY NUMBER(38,0),
    _GOLD_BATCH_ID VARCHAR,
    _SOURCE_SYSTEM VARCHAR,
    _SOURCE_SCHEMA VARCHAR,
    _SOURCE_TABLE VARCHAR,
    _SILVER_RECORD_HASH VARCHAR,
    _RECORD_HASH VARCHAR,
    _GOLD_PROCESSED_AT TIMESTAMP_NTZ,
    EFFECTIVE_FROM TIMESTAMP_NTZ,
    EFFECTIVE_TO TIMESTAMP_NTZ,
    IS_CURRENT BOOLEAN,
    _EVENT_FIELD VARCHAR,
    DATE_KEY NUMBER(38,0)
);
CREATE TABLE IF NOT EXISTS CURATED.FACT_OBSERVATION (
    SURROGATE_KEY NUMBER(38,0),
    _GOLD_BATCH_ID VARCHAR,
    _SOURCE_SYSTEM VARCHAR,
    _SOURCE_SCHEMA VARCHAR,
    _SOURCE_TABLE VARCHAR,
    _SILVER_RECORD_HASH VARCHAR,
    _RECORD_HASH VARCHAR,
    _GOLD_PROCESSED_AT TIMESTAMP_NTZ,
    EFFECTIVE_FROM TIMESTAMP_NTZ,
    EFFECTIVE_TO TIMESTAMP_NTZ,
    IS_CURRENT BOOLEAN,
    _EVENT_FIELD VARCHAR,
    DATE_KEY NUMBER(38,0)
);
CREATE TABLE IF NOT EXISTS CURATED.FACT_PRESCRIPTION (
    SURROGATE_KEY NUMBER(38,0),
    _GOLD_BATCH_ID VARCHAR,
    _SOURCE_SYSTEM VARCHAR,
    _SOURCE_SCHEMA VARCHAR,
    _SOURCE_TABLE VARCHAR,
    _SILVER_RECORD_HASH VARCHAR,
    _RECORD_HASH VARCHAR,
    _GOLD_PROCESSED_AT TIMESTAMP_NTZ,
    EFFECTIVE_FROM TIMESTAMP_NTZ,
    EFFECTIVE_TO TIMESTAMP_NTZ,
    IS_CURRENT BOOLEAN,
    _EVENT_FIELD VARCHAR,
    DATE_KEY NUMBER(38,0)
);
CREATE TABLE IF NOT EXISTS CURATED.BRIDGE_CLAIM_DIAGNOSIS (
    BRIDGE_KEY NUMBER(38,0),
    CLAIM_SK NUMBER(38,0),
    DIAGNOSIS_SK NUMBER(38,0),
    SEQUENCE_NUMBER NUMBER(38,0),
    _GOLD_BATCH_ID VARCHAR,
    EFFECTIVE_FROM TIMESTAMP_NTZ,
    EFFECTIVE_TO TIMESTAMP_NTZ,
    IS_CURRENT BOOLEAN
);
CREATE TABLE IF NOT EXISTS CURATED.BRIDGE_PROVIDER_FACILITY (
    BRIDGE_KEY NUMBER(38,0),
    AFFILIATION_ID NUMBER(38,0),
    PROVIDER_SK NUMBER(38,0),
    FACILITY_SK NUMBER(38,0),
    EFFECTIVE_START_DATE DATE,
    EFFECTIVE_END_DATE DATE,
    _GOLD_BATCH_ID VARCHAR,
    EFFECTIVE_FROM TIMESTAMP_NTZ,
    EFFECTIVE_TO TIMESTAMP_NTZ,
    IS_CURRENT BOOLEAN
);
CREATE TABLE IF NOT EXISTS CURATED.BRIDGE_MEMBER_ENROLLMENT (
    BRIDGE_KEY NUMBER(38,0),
    ENROLLMENT_ID NUMBER(38,0),
    MEMBER_SK NUMBER(38,0),
    PLAN_SK NUMBER(38,0),
    STATUS VARCHAR,
    COVERAGE_START_DATE DATE,
    COVERAGE_END_DATE DATE,
    _GOLD_BATCH_ID VARCHAR,
    EFFECTIVE_FROM TIMESTAMP_NTZ,
    EFFECTIVE_TO TIMESTAMP_NTZ,
    IS_CURRENT BOOLEAN
);

-- -----------------------------------------------------------------------------
-- Operating grants
-- -----------------------------------------------------------------------------
GRANT SELECT, INSERT, UPDATE, DELETE, TRUNCATE ON ALL TABLES IN SCHEMA RAW_STAGE TO ROLE ROLE_HCPIPE_LOADER;
GRANT SELECT, INSERT, UPDATE, DELETE, TRUNCATE ON FUTURE TABLES IN SCHEMA RAW_STAGE TO ROLE ROLE_HCPIPE_LOADER;
GRANT SELECT, INSERT, UPDATE, DELETE ON ALL TABLES IN SCHEMA CURATED TO ROLE ROLE_HCPIPE_LOADER;
GRANT SELECT, INSERT, UPDATE, DELETE ON FUTURE TABLES IN SCHEMA CURATED TO ROLE ROLE_HCPIPE_LOADER;
GRANT SELECT, INSERT, UPDATE, DELETE ON ALL TABLES IN SCHEMA AUDIT TO ROLE ROLE_HCPIPE_LOADER;
GRANT SELECT, INSERT, UPDATE, DELETE ON FUTURE TABLES IN SCHEMA AUDIT TO ROLE ROLE_HCPIPE_LOADER;
GRANT SELECT, INSERT, UPDATE, DELETE ON ALL TABLES IN SCHEMA KPI TO ROLE ROLE_HCPIPE_LOADER;
GRANT SELECT, INSERT, UPDATE, DELETE ON FUTURE TABLES IN SCHEMA KPI TO ROLE ROLE_HCPIPE_LOADER;
GRANT SELECT ON ALL TABLES IN SCHEMA CURATED TO ROLE ROLE_HCPIPE_SECURITY_ADMIN;
GRANT SELECT ON ALL TABLES IN SCHEMA CURATED TO ROLE ROLE_HCPIPE_BI;
